export const Header = () => {
    return (
        <header class="header">
            <div class="logo">
                <span class="course">react course</span>
                <span class="description">user list demo</span>
            </div>
        </header>
    );
};